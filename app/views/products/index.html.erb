 <!-- INDEX PAGE SEALSTEALS.COM -->



<% @products.each do |category, products| %>

  <h3><%= category.name %></h3>
  <ul>
    <% products.each do |product| %>
    <li id="SSProduct" style="height: 350px;">
      <h4><%= link_to product.name, product_path(product.permalink) %></h4>
      <%= image_tag product.default_image.path, :height => '200px' %>
      <!-- <p><%= product.short_description %></p> -->
      <p><b>Price:</b> <%= number_to_currency product.price %></p>
    </li>
    <% end %>
  </ul>
<% end %>